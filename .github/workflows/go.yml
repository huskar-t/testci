name: Go

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: arm64v8/ubuntu:bionic
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          path: 'blm3'
      - name: checkout TDengine
        uses: actions/checkout@v2
        with:
          repository: 'taosdata/TDengine'
          path: 'TDengine'
          ref: 'develop'
      - name: install build-essential
        run: sudo apt-get update && sudo apt-get install -y build-essential curl gcc cmake

      - name: install TDengine
        run: cd TDengine && mkdir debug && cd debug && cmake .. -DBUILD_HTTP=true -DBUILD_JDBC=false && make -j 16 && sudo make install

      - name: start taosd
        run: nohup sudo taosd &

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.14

      - name: Test
        run: go test -v ./blm3/...
